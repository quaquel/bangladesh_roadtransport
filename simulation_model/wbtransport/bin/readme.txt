The whole binary has been added as a multipart rar-file. 
In addition, the CentroidRoutes folder contains the binary as separate folders and files.

Run the model with the command:
java -jar CentroidRoutes.jar

Use Java 8 (1.8.0_xxx) for this project.